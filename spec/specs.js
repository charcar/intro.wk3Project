describe('pingPong', function() {
  it("is a non integer value", function() {
    expect(pingPong("foobar")).to.equal(false);
  });

  it("is an integer value", function() {
    expect(pingPong(20)).to.

  })


  });
